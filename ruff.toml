[lint]
select = ["E", "F", "W", "I", "UP", "S", "B"]
ignore = ["S101"]  # assert is fine in non-production code

[lint.per-file-ignores]
"scripts/tests/*" = ["S", "B"]  # security/bugbear rules relaxed in tests

[format]
line-length = 100
